name: Deploy website to GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - website/**
      - .github/workflows/website-pages.yml
  workflow_dispatch:
  release:
    types:
      - published

permissions:
  contents: read

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    name: Test, build, and deploy website
    runs-on: ubuntu-22.04
    timeout-minutes: 10
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # Release events otherwise check out the tag and can redeploy stale website sources.
          ref: main
          persist-credentials: false

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: website/package-lock.json

      - name: Configure GitHub Pages
        uses: actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d # v6.0.0

      - name: Install website dependencies
        run: npm ci --prefix website

      - name: Test website
        run: npm test --prefix website

      - name: Resolve latest website release
        id: website_release
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: node website/scripts/resolve-release.mjs

      - name: Build website
        env:
          GAJAE_WEBSITE_RELEASE_PUBLISHED_LABEL: >-
            ${{ steps.website_release.outputs.release_published_label }}
          GAJAE_WEBSITE_RELEASE_TAG: ${{ steps.website_release.outputs.release_tag }}
        run: npm run build --prefix website

      - name: Upload website artifact
        uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        with:
          path: website/dist

      - name: Deploy website
        id: deployment
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5.0.0
